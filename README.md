<!-- PROJECT LOGO OR BANNER -->
<div align="center">
  <h1> DevStack</h1>
  <p><strong>Compare frontend, backend, and database tools side by side, and pick the perfect stack for your project.</strong></p>
</div>

 **Tech Stack Used**
*  **React**
*  **Tailwind-CSS**
*  **JavaScript**

------------------------------
 **Project Feature**
*  **[Feature 1 Add to Stack]:** [Choose your favorite tools and add them to your stack.]
*  **[Feature 2 Remove ]:** [Easily remove any single tool from your stack.]
*  **[Feature 3 Remove All ]:** [Remove all tools from your stack with just one click.]
-------------------------------



React Question Answarer 
* **what is jsx:** jsx is javascript xml and look like html tag some behaviour like html , **Why use react** : its make user interface commponent base and using complex javascript methods to create html element 

* **What is the difference between props and state?**  props is propertice props data passed into a component and , state is a data managed inside component used to handle dynamic changing data 

* **What does the useState hook do, and where did you use it in this project?** useState is function remember pieces of information and update the UI whenever that information changes. **Where it is used** input fields & forms Keeping track of dynamic lists, such as adding or removing items from a list or stack.

* **What id does** : useEffect performs side effects like fetching data **why load Json data**  data takes time and must happen after rendering to prevent block the ui and avoid infinite rendering loops


* **Why map() lists need a unique key prop**  it helps react track which item change were added were remove 

* **what conditonal rendering** displaying deffernet UI elements based on whether a condition is true or false.

example: {items.length === 0 && <p>The stack is empty</p>}


* **Data passing between Parent and Child** Parent ==> Child: Passed down using props  title="Hello" | Child ==> Parent The parent passes a callback function via props, and the child calls it with data ( props.onSelect(data)).



